import type { NextPage } from "next";
import { NavbarComponent } from "../components";

const Home: NextPage = () => {
  return (
    <header>
      <NavbarComponent />
    </header>
  );
};

export default Home;
